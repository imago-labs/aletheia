{
  "name": "aletheia-contracts",
  "version": "0.1.0",
  "description": "ALETHEIA Pool smart contract -- discretionary mutual on Base Sepolia",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat",
    "deploy:base-sepolia": "hardhat run scripts/deploy.js --network base-sepolia",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.5"
  }
}
